#include "factory.h"
//#include "constants.h"

//Motor mLeftMotor(PORT_B);
//History mHMotorR(mRightMotor);

float data_3[3] = {0.0};
float data_10[10] = {0.0};

/*
// デバイスオブジェクトの生成
TouchSensor mTouchSensor(PORT_1);
LightSensor mLightSensor(PORT_3);
GyroSensor mGyroSensor(PORT_4);
Motor mLeftMotor(PORT_B);
Motor mRightMotor(PORT_C);
Lcd mLcd;
Clock mClock;
Nxt mNxt;
Bluetooth mBluetooth;
Daq mDaq(mBluetooth);

// GPS/Historyとそのアダプタ群
Gps mGps(mLeftMotor, mRightMotor);
XCoordinate mXCoordinate(mGps);
YCoordinate mYCoordinate(mGps);
Angle mAngle(mGps);
Distance mDistance(mGps);
//History mHistory
History mHMotorR(mRightMotor);
History mHMotorL(mLeftMotor);
History mHLight(mLightSensor);
History mHAccR(mRightMotor); //角加速度どうやって作る?
History mHAccL(mLeftMotor);
//何のヒストリオブジェクトが必要？
// その他
Activator mActivator(mLeftMotor, mRightMotor, mGyroSensor, mNxt);
Pid mLightPid(LIGHT_PID_KP, LIGHT_PID_KI, LIGHT_PID_KD);
Pid mAnglePid(ANGLE_PID_KP, ANGLE_PID_KI, ANGLE_PID_KD);
//Skill
LineTrace mLineTrace(BLACK, WHITE, LINE_THRESHOLD);
AngleTrace mAngleTrace;
// Driver
NormalDriver mNormalDriver;
// Navigator
#ifdef INCOURSE
InCourse mInCourse;
Robot mRobot(mInCourse);
#endif
#ifdef OUTCOURSE
OutCourse mOutCourse;
Robot mRobot(mOutCourse);
#endif
*/
